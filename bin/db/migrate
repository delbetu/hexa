#!/bin/bash

case $RACK_ENV in
  "test")
    source .env.test
    ;;
  *)
    source .env
    ;;
esac

sequel -E -m $APP_ROOT/db/migrate $DATABASE_URL

echo "Project location ${APP_ROOT}"
echo "Database ${DATABASE_URL}"
